# URL Status Checker

This project reads URLs from a CSV file and checks their HTTP status codes.

## Features
- Skips empty rows
- Handles request errors
- Prints status codes for each URL

## How to Run

1. Install dependencies:
   pip install requests

2. Run the script:
   python intern.py
